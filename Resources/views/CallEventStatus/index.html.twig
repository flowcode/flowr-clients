{% extends "FlowerCoreBundle::layout.html.twig" %}

{% block title %}{{ '%entity% List'|trans({'%entity%': 'CallEventStatus'}, 'CallEventStatus') }} - {{ parent() }}{% endblock %}

{% block app_menu %}
    {{ include("FlowerCoreBundle:Default:admin_nav.html.twig") }}
{% endblock %}

{% block body %}
    <section class="content-header">
        <h1><a href="{{ path('admin_dash') }}"> {{ 'Settings'|trans}}</a>
            /
            {{ '%entity% List'|trans({'%entity%': 'CallEventStatus'}, 'CallEventStatus') }}
        </h1>
    </section>
    <section class="content">    
        <div class="box">
            <div class="box-header">
                <div class="box-tools pull-right">                    <a class="btn btn-link btn-sm" href={{ path('calleventstatus_new') }}>
                        <i class="fa fa-plus"></i> {{ 'Create a new %entity%'|trans({'%entity%': 'CallEventStatus'}, 'CallEventStatus') }}
                    </a>
                            </div>
            </div>
            <div class="box-body">    
            <table class="table table-hover records_list table-condensed">
                <thead>
                    <tr>
                        <th scope="col">Id</th>
                        <th scope="col">Name</th>
                        <th scope="col">Finished</th>
                        <th scope="col">{{ 'Actions'|trans({}, 'CallEventStatus') }}</th>
                    </tr>
                </thead>
                <tbody>
                    {% for calleventstatus in paginator %}
                        <tr>
                            <td><a href="{{ path('calleventstatus_show', {id: calleventstatus.id}) }}">{{ calleventstatus.id }}</a></td>
                            <td><a href="{{ path('calleventstatus_show', {id: calleventstatus.id}) }}">{{ calleventstatus.name }}</a></td>
                            <td>{% if calleventstatus.finished %}<i class="fa fa-check-square-o"></i>{% else %}<i class="fa fa-square-o"></i>{% endif %}</td>
                            <td>
                        <div class="btn-group">
                            <a class="btn btn-link  btn-sm" href="{{ path('calleventstatus_show', {id: calleventstatus.id}) }}">
                                {{ 'show'|trans({}, 'CallEventStatus') }}
                            </a>
                            <a class="btn btn-link  btn-sm" href="{{ path('calleventstatus_edit', {id: calleventstatus.id}) }}">
                                 {{ 'edit'|trans({}, 'CallEventStatus') }}
                            </a>
                        </div>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
            
            {{ knp_pagination_render(paginator) }}
            </div>
        <div class="box-footer">

        </div>
        
    </section>        

{% endblock %}
