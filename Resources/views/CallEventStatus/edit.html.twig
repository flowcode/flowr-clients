{% extends "FlowerCoreBundle::layout.html.twig" %}

{% block title %}{{ '%entity% edit'|trans({'%entity%': 'CallEventStatus'}, 'CallEventStatus') }} - {{ parent() }}{% endblock %}

{% block app_menu %}
    {{ include("FlowerCoreBundle:Default:admin_nav.html.twig") }}
{% endblock %}

{% block body %}
    <section class="content-header">

        <h1><a href="{{ path('admin_dash') }}"> {{ 'Settings'|trans}}</a>
            /
            <a href="{{ path('calleventstatus') }}"> {{ '%entity% list'|trans({'%entity%': 'CallEventStatus'}, 'CallEventStatus') }}</a>
            /
            {{ 'CallEventStatus' | trans({}, 'CallEventStatus')}}
        </h1>
    </section>    
    <section class="content">
        <div class="box">
            <div class="box-body">

            {{ form_start(edit_form) }}
                {{ form_widget(edit_form) }}
                <p>
                    <button class="btn btn-primary" type="submit">{{ 'Edit'|trans({}, 'CallEventStatus') }}</button>
                </p>
            {{ form_end(edit_form) }}
            </div>
            <div class="box-footer">
           
            </div>
        </div>
    </section>
{% endblock %}


