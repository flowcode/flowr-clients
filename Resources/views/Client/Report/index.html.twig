{% extends "FlowerClientsBundle:Client:layout.html.twig" %}

{% block title %}{{ '%entity% list'|trans({'%entity%': 'Opportunity'}, 'Opportunity') }} - {{ parent() }}{% endblock %}

{% block app_menu %}
    {{ include("FlowerClientsBundle:Client:appnav.html.twig",{'app_report':true}) }}
{% endblock %}

{% block body %}
    <section class="content-header">
        <h1>
            {{ 'reports'|trans|title }}
        </h1>
    </section>
    <section class="content">
        <div class="box">
            <div class="box-header">
                <div class="box-tools pull-right">
                </div>
            </div>
            <div class="box-body">
                <div class="callout callout-info col-sm-6">
                    ¡En breve!
                </div>

            </div>
            <div class="box-footer">

            </div>
        </div>

    </section>

{% endblock %}