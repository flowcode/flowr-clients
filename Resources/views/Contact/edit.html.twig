{% extends "FlowerCoreBundle::layout.html.twig" %}

{% block title %}{{ '%entity% edit'|trans({'%entity%': 'Contact'}, 'Contact') }} - {{ parent() }}{% endblock %}

{% block app_menu %}
    {{ include("FlowerClientsBundle::appnav.html.twig", { 'app_contact': true }) }}
{% endblock %}

{% block body %}
<section class="content-header">
    <h1>{{ contact.happyname | trans({}, 'Contact') }}</h1>
</section>
<section class="content-header">
    <section class="content">
        <div class="box">
            <div class="box-header">
                <h3 class="box-title">
                    {{ contact.happyname | trans({}, 'Contact') }}
                </h3>
            </div>
            <div class="box-body">

                {{ form_start(edit_form) }}
                {{ form_widget(edit_form) }}
                <p>
                    <button class="btn btn-primary" type="submit"><i
                                class="fa fa-edit"></i> {{ 'Edit'|trans({}, 'Contact') }}</button>
                </p>
                {{ form_end(edit_form) }}
            </div>
            <div class="box-footer">
                {{ form_start(delete_form) }}
                {{ form_widget(delete_form) }}

                <div class="btn-group record_actions">

                    <button class="btn btn-danger" type="submit"><i
                                class="fa fa-trash-o"></i> {{ 'Delete'|trans({}, 'Contact') }}</button>

                </div>

                <input name="modal" type="hidden" value="0">
                {{ form_end(delete_form) }}

                <div id="delete_confirm" class="modal fade">
                    <div class="modal-dialog">
                        <div class="modal-content">
                            <div class="modal-header">
                                <button type="button" class="close" data-dismiss="modal"
                                        aria-hidden="true">&times;</button>
                                <h3>{{ 'Confirm delete'|trans({}, 'Contact') }}</h3>
                            </div>
                            <div class="modal-body">
                                <p>{{ 'You are about to delete an item'|trans({}, 'Contact') }}
                                    , {{ 'this procedure is irreversible'|trans({}, 'Contact') }}.</p>
                                <p>{{ 'Do you want to proceed?'|trans({}, 'Contact') }}</p>
                            </div>
                            <div class="modal-footer">
                                <button class="btn btn-danger">{{ 'Yes'|trans({}, 'Contact') }}</button>
                                <button class="btn btn-default"
                                        data-dismiss="modal">{{ 'No'|trans({}, 'Contact') }}</button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
    {% endblock %}


